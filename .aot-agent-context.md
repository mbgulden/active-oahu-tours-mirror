# Active Oahu Tours — Shared Agent Context
# Loaded by all Kai sub-agents on every execution.
# Version: 2026-06-13 | Updated by: Fred

## Site Structure
- Root: /home/ubuntu/work/active-oahu-static/
- Deployment branch: master → Cloudflare Pages (activeoahutours.com)
- Preview branch: main → CF Pages preview (*.pages.dev)
- Pages: 130 English + 83 Japanese (/ja/ directory)
- Static HTML — no build step, no WordPress, no JS framework

## Directory Layout
```
site/
├── index.html              # Homepage
├── activities/             # Tour pages (kayak, snorkel, e-bike, etc.)
├── rentals/                # Rental pages (kayak, paddleboard, etc.)
├── guides/                 # Guide pages (sharks cove, etc.)
├── ja/                     # Japanese translations (mirrors EN structure)
├── _redirects              # Cloudflare Pages redirect rules (301/302)
├── css/                    # (only if created — currently inline styles)
└── js/                     # (does NOT exist — JS is inline)
```

## Brand Voice
- Authentic, local-first, adventure-oriented
- "We paddle these waters every day" — NOT corporate
- Knowledgable but approachable — like a local friend giving advice
- Hawaiian place names used correctly with diacritics (Mokulua, Kailua, etc.)

## Design Tokens
- Primary blue: #0B4A97
- Font: system stack (no custom webfonts loaded)
- Mobile-first: breakpoints at 768px, 1024px, 1280px
- Nav: sticky, hamburger at < 1024px

## Active State
- Branch divergence: main and master have diverged — push to BOTH
- CF Pages deploy delay: 30-120 seconds after push
- Broken links: 15 validated high-traffic issues remain
- Schema: TouristTrip for tours, Product for rentals, FAQPage for guides

## Sub-Agent Fleet
| Agent | Label | Domain |
|-------|-------|--------|
| Kai-CSS | agent:kai-css | CSS, styling, layout, accessibility |
| Kai-Content | agent:kai-content | Content, SEO, schema, translations |
| Kai-JS | agent:kai-js | JavaScript, interactivity, redirects |
| Kai | agent:kai | Orchestrator — decomposes, reviews, approves |

## Review Pipeline
Sub-agent executes → self-reviews → swaps to agent:agy → AGY peer review → agent:done
